import type { TRPCContextInnerWithSession } from '@/server/create-context';
import type { {{ schemaName }} } from './{{ baseHandlerFile }}-schema'
{{#if handlerReturnType}}
import { {{ handleDataType }} } from "@repo/common-types";
{{/if}}

type {{ handlerOptions }} = {
    ctx: TRPCContextInnerWithSession;
    input: {{ schemaName }};
}

export const {{ handlerName }} = async ({ ctx, input }: {{handlerOptions}}){{ handlerReturnType }} => {

    const { prisma, session } = ctx;
    
    const res = {{ handlerContent }}
    return res;
}

export type {{ handlerResponse }} = Awaited<ReturnType<typeof {{ handlerName }}>>;